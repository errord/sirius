2009-7-12
	sarray增加remove方法

2009-7-7
	优化suffixtree_check_node函数中edge查找代码
		1. 加入单独的cache机制.已经试验,效率降低近3倍.原因是加入cache提高查找效率的同时也加入了更多的维护cache的操作
		   如:加入edge时需要同时加入cache,删除时也需要在cache删除.并且每个edge都需要维护cache.维护成本非常大.并且非
		   常消耗内存.
		2. 现在存储edge是通过链表完成的.查找是的时间复杂度为O(n).可以改为hash.待试验.

2009-7-2
	加入插件机制.
	在插件机制的基础上加入编码机制,可以向suffixtree中插入
	各类编码的文本.显示时可以根据插件进行转码.

